export default class LokkaTransport {
  /*eslint-disable */
  send(rawQuery, variables, operationName, context) {
    throw new Error('not implemented!');
    // return new Promise();
  }
  /*eslint-enable */
}
